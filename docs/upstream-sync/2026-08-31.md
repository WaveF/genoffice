# 上游适配台账：2026-08-31

## 审计范围

- NexOffice 上游基线：`da3b1ca7809a511a33d7f5461d8f04d33f48ab0f`
- 审计的上游目标：`3548a808170616c2b13b1dc7c6c81cb49205d504`
- 范围：14 个提交，2026-08-25 至 2026-08-31。
- 集成目标：`mcp-only`。本轮实施使用短期分支 `upstream-2026-08-core-fixes`。

## 本轮已拆解条目

- [x] **UP-03 — Markdown 关闭/保存竞态**  
  上游：`f81dd3d5b907d73de99b7ffa0e20ebd626cdb159` (`fix(markdown): wait for in-flight saves during close instead of silently failing`)。  
  问题：关闭确认恰逢自动保存时，显式保存会直接失败，导致“保存并关闭”无法完成。  
  决定：适配采纳，P0。等待保存结束；若文档已不脏则直接确认关闭，否则执行显式保存。

- [x] **UP-01 — Sheets 大视口流式读取分批**  
  上游：`bc1dceb85d8241262c7d27b5f2e214d74edc3da6` (`fix(sheets): row-batch over-cap sidecar reads so streaming survives big viewports`)。  
  问题：无结构编辑的远距离/大视口读取可超过 sidecar 单次 cell 上限，滚动持续失败。  
  决定：适配采纳，P1。按行分批读取并在索引未覆盖后续批次时早停。

- [x] **UP-02 — Sheets 查找排除筛选隐藏行**  
  上游：`7a814dbd2327059f3f28fbaaa7f79f16f9245b37` (`fix(sheets): keep filter-hidden rows out of full-sheet find results`)。  
  问题：流式文件侧查找可能返回已经被活动筛选隐藏的行，与当前窗口内的查找结果不一致。  
  决定：适配采纳，P1。以当前 Workbook 的筛选模型过滤候选行；模型不可用时保持 fail-open。

- [x] **UP-09 — 项目操作失败反馈**  
  上游：`9711a4507cf70d32610ffd423b5915c423f4a682` (`fix(shell): surface project IPC failures and AI settings save/test errors`)。  
  问题：本地项目创建、重命名、删除的 IPC 失败会形成未处理拒绝，界面表现为无反馈。  
  决定：仅适配项目操作错误反馈，P2；不恢复 AI 设置相关逻辑。

- [x] **UP-11 — 拖放本地文档打开**  
  上游：`607c77086f8827af274cd07ccb3692d3e755be14` (`feat(shell): open documents dropped onto the window`)。  
  问题：导航保护会吞掉拖入应用窗口的本地文档，用户无法直接打开。  
  决定：适配采纳，P2。各 surface preload 只上报受支持文件，Shell 复用既有打开路由；不把拖放实现接入任何 AI 附件路径。

- [x] **UP-14 — Slides 旋转表格单元格双击编辑**  
  上游：`3548a808170616c2b13b1dc7c6c81cb49205d504` (`fix: rotated table cell dblclick-to-edit in slides`)。  
  问题：当前命中测试显式跳过旋转表格，导致无法双击进入单元格编辑。  
  决定：适配采纳，P2。按表格本地坐标反变换命中，再生成正确的编辑覆盖位置。

## 已完成审计、不进入本轮实施

- [x] **UP-04 — AI Provider 中止 SSE 时释放 reader**  
  上游：`b1a01e24da996b6c3b634e317961067d723f661b`。  
  决定：明确不采纳；强耦合已下线的内置 AI Provider。

- [x] **UP-05 — AI Provider 容忍畸形 SSE 帧**  
  上游：`72a4a42db14dfa9c80dcfd6e499a0c1fd81cd399`。  
  决定：明确不采纳；强耦合已下线的内置 AI Provider。

- [x] **UP-06 — Slides AI 布局审计的水平溢出**  
  上游：`9792915878309354ce557862d686f41b4a069bb1`。  
  决定：本轮不采纳；可作为未来独立 Slides/MCP 布局预检的参考，不引入内置 AI 审计链。

- [x] **UP-07 — Slides Agent 写入演讲者备注**  
  上游：`2e3e97f53a0b7ea98fb09905bb1e2689d099a538`。  
  决定：当前核心 `setNotes` 操作已存在；后续只需审计其 MCP 暴露，不移植上游 AiPanel/AgentSkill 链路。

- [x] **UP-08 — 流式 Sheets 全文件错误检查**  
  上游：`7eb5d59b59cc0765741645584eab2e587e6597f2`。  
  决定：暂缓；当前缺少可独立适配的完整“错误检查”功能面，应作为未来 Sheets 能力项单独设计。

- [x] **UP-10 — 2026-08-26 聚合 snapshot**  
  上游：`0a2c25d483124bbb148d979fb3a30612298e9cdc`。  
  决定：不作为整体任务；其中 Docs 渲染、Sheets CSV/验证、Slides 字体功能须按后续 PR/文件继续拆解，AI 文本布局不采纳。

- [x] **UP-12 — 未保存 Markdown 粘贴图片前提示保存**  
  上游：`583a045212f871943afb8ca4503fcb5ddf99a23f`。  
  决定：暂缓；可改善粘贴/拖放图片体验，但必须保持未保存文档的插图按钮禁用策略，留待 Markdown 素材交互专项评估。

- [x] **UP-13 — 2026-08-30 聚合 snapshot**  
  上游：`2239cce9e7f4ad07c2e8e327051ff1b8dda4562c`。  
  决定：不作为整体任务；Docs 保真、Sheets 大文件正确性、Slides RTL/渲染需逐项拆解。内置 AI、远程字体目录与上游服务相关内容默认不采纳。

## 本轮验收记录

完成每个未勾选条目后，在原条目下补充：NexOffice 提交、自动化测试、手工验证和剩余风险。所有六项完成并验证后才将本轮实施分支合入 `mcp-only`。

六项已于 NexOffice 提交 `5fb3669` 完成。验证证据：Shell 全量测试 247 项通过；Sheets Rust 原生测试 108 项与 `lazy-find` / 读取分批定向测试 29 项通过；Slides `table-hit` 2 项通过；Markdown 全量测试 115 项通过；Shell、Sheets、Slides、Markdown typecheck 均通过。拖放打开覆盖各 surface preload 与 Shell 的受支持格式路由，仍建议在合并后手工拖放一次每种主要文件类型进行烟测。
