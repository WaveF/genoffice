{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node"],
    "paths": {
      // resolve workspace types from this checkout's sources: in a git worktree
      // node_modules is a symlink into the main checkout, so bare specifiers
      // would typecheck against the other checkout's (possibly stale) types
      "@nexoffice/docx-engine": ["../../packages/docx-engine/src/index.ts"],
      "@nexoffice/font-metrics": ["../../packages/font-metrics/src/index.ts"],
      "@nexoffice/electron-utils": ["../../packages/electron-utils/src/index.ts"],
      "@nexoffice/i18n": ["../../packages/i18n/src/index.ts"],
      "@nexoffice/ui": ["../../packages/ui/src/index.ts"]
    }
  },
  "include": ["src", "tests", "electron.vite.config.ts", "vitest.config.ts"]
}
